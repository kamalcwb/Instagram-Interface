// complete as variaveis necessarias
var n = parseInt();
var kg = parseFloat()

while (n > 0) { //complete o codigo
    var c = parseFloat();
    let dias = n; //atribua o valor correto a variavel
    while (kg >= 1) { // complete o while
        let kg = kg / 2
        let dias = dias++
    }
    console.log(dias` dias`); //complete o código para a saída
}